# MENSAGENS DESCRITIVAS DOS EXERCICIOS LOGICA PYTHON

Atividade 1 - O programa foi desenvolvido em Python seguindo uma estrutura modular, com a função main() que contém toda a lógica de execução. A resolução atende a todos os requisitos solicitados, com foco em validação de dados e apresentação organizada

Atividade 2 — O programa foi desenvolvido em Python utilizando uma abordagem direta e funcional, com entrada de dados via função `input()` convertida para `float()`, permitindo a manipulação de números reais. A resolução atende a todos os requisitos solicitados, implementando as seis operações matemáticas solicitadas (adição, subtração, multiplicação, divisão, divisão inteira, resto da divisão e potenciação). O código apresenta uma estrutura condicional `if` que verifica se o segundo número é igual a zero, impedindo a execução das divisões e exibindo a mensagem "Divisão por zero não permitida" para essas operações específicas. As demais operações são executadas normalmente, garantindo a robustez do programa. A saída é apresentada de forma organizada com separadores visuais, facilitando a leitura e compreensão dos resultados pelo usuário.

Atividade 3 — O programa foi desenvolvido em Python com uma estrutura simples e objetiva, utilizando a função `float()` para receber valores decimais de temperatura em graus Celsius. A resolução atende integralmente aos requisitos, aplicando as fórmulas de conversão fornecidas no enunciado: `Fahrenheit = Celsius × 9/5 + 32` e `Kelvin = Celsius + 273,15`. O código utiliza formatação com `:.2f` para exibir todos os valores com duas casas decimais, conforme exigido. A apresentação dos resultados é organizada com separadores visuais e inclui os respectivos símbolos das unidades (°C, °F e K), garantindo clareza e padronização na saída. O programa funciona para qualquer temperatura real, incluindo valores negativos e decimais.

Atividade 4 — O programa foi desenvolvido em Python com uma abordagem modular, implementando a função `validar_nota()` que emprega um loop `while True` com `try/except` para garantir que as notas inseridas estejam no intervalo válido de 0 a 10. A resolução atende a todos os requisitos solicitados, realizando a validação individual de cada nota e solicitando nova entrada em caso de erro. O cálculo da média aritmética é realizado através da soma das três notas dividida por 3, e a determinação da situação acadêmica utiliza uma estrutura condicional `if/elif/else` conforme os critérios estabelecidos (≥ 7,0 Aprovado; entre 5,0 e 6,9 Recuperação; < 5,0 Reprovado). Os resultados são exibidos com as notas em uma casa decimal, a média com duas casas decimais e a situação final claramente identificada.

Atividade 5 — O programa foi desenvolvido em Python com uma estrutura robusta de validação, implementando a função `validar_idade()` que utiliza um loop `while True` com `try/except` para garantir que a idade seja um número inteiro não negativo. A resolução atende integralmente aos requisitos, impedindo a entrada de idades negativas e valores não numéricos. A classificação etária é determinada por meio de uma estrutura condicional `if/elif/else` que abrange todas as faixas etárias solicitadas (até 12 anos: Criança; 13 a 17 anos: Adolescente; 18 a 59 anos: Adulto; 60 anos ou mais: Idoso). O programa exibe a idade informada e a classificação correspondente em uma mensagem clara e objetiva, com separadores visuais que organizam a saída de forma profissional.

Atividade 6 — O programa foi desenvolvido em Python com uma abordagem que implementa a lógica de comparação manualmente, sem utilizar as funções nativas `max()` e `min()` como exigido pela restrição. A resolução atende a todos os requisitos, utilizando estruturas condicionais aninhadas com operadores relacionais (`>`, `<`) e lógicos (`and`, `or`) para determinar o maior, o menor e o número intermediário (mediana). O código inclui uma validação que verifica se os três números são distintos, exibindo uma mensagem de erro caso contrário. Os resultados são apresentados de forma organizada, mostrando os números informados, o maior, o menor, a mediana e a ordenação completa em ordem crescente e decrescente, demonstrando plenamente a compreensão das estruturas condicionais complexas.

Atividade 7 — O programa foi desenvolvido em Python com uma estrutura concisa e eficiente, realizando a análise do sinal e da paridade em uma única execução. A resolução atende aos requisitos solicitados, determinando se o número é positivo, negativo ou nulo através de uma estrutura condicional `if/elif/else`, e verificando a paridade utilizando o operador módulo `%` para identificar se o número é par ou ímpar. O código trata adequadamente o número zero, classificando-o como nulo e par. A saída é apresentada em uma única mensagem coesa conforme o exemplo fornecido ("O número X é positivo e par"), demonstrando a capacidade de combinar múltiplas análises em um resultado integrado e de fácil compreensão.

Atividade 8 — O programa foi desenvolvido em Python com uma estrutura de repetição `for` para capturar os 10 números inteiros solicitados, conforme requisito obrigatório. A resolução atende a todos os requisitos, processando os dados em tempo real com validação de entrada através de `try/except`. O código implementa todas as análises estatísticas solicitadas: soma total, contagem de números positivos, contagem de números negativos, contagem de números pares, contagem de números ímpares e média aritmética com duas casas decimais. O programa trata adequadamente o número zero, considerando-o par e não o contabilizando como positivo ou negativo. Os resultados são apresentados em um relatório organizado com separadores visuais, garantindo clareza e profissionalismo na saída.

Atividade 9 — O programa foi desenvolvido em Python utilizando obrigatoriamente uma estrutura de repetição `for` para gerar a tabuada de multiplicação, conforme exigido pela questão. A resolução atende a todos os requisitos, apresentando a tabuada no intervalo de 1 a 10 com formatação padronizada. O código inclui validação de entrada com `try/except` para garantir que o usuário informe um número inteiro válido, e pode ser utilizado tanto com números positivos quanto negativos. A saída segue o formato solicitado ("5 x 1 = 5, 5 x 2 = 10, ... 5 x 10 = 50"), com alinhamento visual que facilita a leitura. Uma versão alternativa com estrutura `while` também foi disponibilizada, demonstrando a flexibilidade do programador com diferentes estruturas de repetição.

Atividade 10 — O programa foi desenvolvido em Python utilizando uma lista para armazenar as 7 temperaturas registradas ao longo da semana, com validação de entrada para garantir que o usuário informe valores numéricos reais. A resolução atende a todos os requisitos solicitados, exibindo todas as temperaturas registradas com seus respectivos dias, identificando a maior e a menor temperatura, calculando a temperatura média com duas casas decimais, e contando quantos dias tiveram temperatura acima da média. O código utiliza funções nativas como `max()`, `min()`, `sum()` e `len()` para otimizar o processamento, além de uma estrutura de repetição para a contagem de dias acima da média. Os resultados são apresentados de forma organizada com separadores visuais, incluindo uma versão alternativa com list comprehension para maior concisão.

Atividade 11 — O programa foi desenvolvido em Python com uma abordagem funcional que permite ao usuário informar 10 números inteiros, armazenando-os em uma lista. A resolução atende a todos os requisitos, gerando um relatório completo que inclui os números originalmente informados, a separação em pares e ímpares, a soma, a média com duas casas decimais, o maior e o menor valor. O código utiliza validação de entrada com `try/except` para garantir a integridade dos dados, e emprega funções nativas como `max()`, `min()`, `sum()` e `len()` para simplificar as operações, embora a lógica manual também pudesse ser implementada conforme a preferência do aluno. A saída é apresentada de forma clara e organizada, facilitando a compreensão dos resultados estatísticos.

Atividade 12 — O programa foi desenvolvido em Python utilizando uma lista de dicionários como estrutura de dados principal, conforme requisito obrigatório da questão. A resolução atende a todos os requisitos, permitindo o cadastro de pelo menos 5 produtos com informações de nome, preço unitário e quantidade disponível em estoque. O código implementa validações para garantir que preço e quantidade não sejam negativos, e exibe todos os produtos cadastrados em formato de tabela organizada. O programa calcula o valor total do estoque através da soma de (preço × quantidade) para todos os produtos, e identifica o produto com o maior preço unitário por meio de uma estrutura de comparação manual. A saída é apresentada de forma profissional com separadores e alinhamento adequado.

Atividade 13 — O programa foi desenvolvido em Python utilizando uma lista de dicionários para armazenar os contatos, conforme requisito obrigatório da questão. A resolução atende a todos os requisitos, permitindo o cadastro de pelo menos 5 contatos com nome, telefone e e-mail. O código exibe todos os contatos cadastrados em formato de tabela organizada, e implementa uma funcionalidade de consulta que busca um contato pelo nome (com tratamento de case insensitive utilizando `.lower()`). Se o contato for encontrado, o programa exibe todos os seus dados completos; caso contrário, exibe a mensagem "Contato não encontrado." A estrutura do código é modular e de fácil compreensão, demonstrando o uso adequado de listas e dicionários para gerenciamento de dados.

Atividade 14 — O programa foi desenvolvido em Python com uma arquitetura modular baseada em funções, conforme recomendado pela questão, utilizando uma lista de dicionários para armazenar os estudantes. A resolução atende a todos os requisitos, implementando funções específicas para validação de notas (`validar_nota()`), cálculo de média (`calcular_media()`), determinação de situação (`determinar_situacao()`) e cadastro de estudante (`cadastrar_estudante()`). O programa permite o cadastro de pelo menos 5 estudantes, cada um com nome, três notas (validadas entre 0 e 10), média calculada automaticamente e situação acadêmica (Aprovado, Recuperação ou Reprovado). Ao final, o sistema exibe todos os estudantes com suas médias, identifica o estudante com maior e menor média, e contabiliza a quantidade de estudantes por situação acadêmica, apresentando estatísticas completas da turma com formatação profissional.

Atividade 15 — O programa foi desenvolvido em Python utilizando uma lista de dicionários para armazenar informações sobre cidades brasileiras, conforme requisito obrigatório. A resolução atende a todos os requisitos, permitindo o cadastro de pelo menos 5 cidades com nome, estado (sigla de 2 letras) e população estimada. O código implementa validações para garantir que o estado tenha exatamente 2 letras e que a população seja um número não negativo. Ao final, o programa identifica e exibe a cidade com maior população, a com menor população, a população total das cidades cadastradas e a média populacional com duas casas decimais. Os dados são apresentados de forma organizada, com a população formatada com separadores de milhar para facilitar a leitura, demonstrando uma aplicação prática de análise de dados com estruturas complexas.

Atividade 16 — O programa foi desenvolvido em Python com uma estrutura modular baseada em funções, implementando um menu interativo para gerenciamento de números. A resolução atende a todos os requisitos, com funções específicas para cada operação: `cadastrar_numero()`, `listar_numeros()`, `exibir_maior()`, `exibir_menor()` e `calcular_media()`. O sistema permanece em execução até que o usuário selecione a opção 0, utilizando um loop `while True` com estruturas condicionais para controle de fluxo. As opções 3, 4 e 5 verificam se há pelo menos um número cadastrado antes de executar, exibindo a mensagem "Nenhum número cadastrado" quando necessário. O código utiliza a lista `numeros` como estrutura de dados principal, com validação de entrada para garantir que apenas números sejam cadastrados, oferecendo uma experiência de usuário interativa e robusta.

Atividade 17 — O programa foi desenvolvido em Python utilizando o módulo `math` da biblioteca padrão, conforme requisito obrigatório da questão. A resolução atende a todos os requisitos, solicitando um número real e apresentando a raiz quadrada (com verificação para números não negativos), o valor absoluto (módulo), o arredondamento para cima (teto), o arredondamento para baixo (piso) e o fatorial (com validação para números inteiros e não negativos). O código demonstra o uso adequado das funções `math.sqrt()`, `math.fabs()`, `math.ceil()`, `math.floor()` e `math.factorial()`, além de tratar adequadamente os casos especiais onde os cálculos não são possíveis, exibindo mensagens informativas. Os resultados são apresentados com formatação adequada, incluindo quatro casas decimais para valores reais e mensagens claras para casos especiais.

Atividade 18 — O programa foi desenvolvido em Python utilizando o módulo `random` da biblioteca padrão para simular lançamentos de dados, conforme requisito obrigatório. A resolução atende a todos os requisitos, implementando duas partes distintas: a Parte 1 realiza um lançamento único de dois dados (faces 1 a 6), exibindo os resultados individuais e a soma; a Parte 2 realiza 10 lançamentos, armazenando os resultados em uma lista de tuplas e contando quantas vezes a soma dos dados foi igual a 7. O código utiliza `random.randint(1, 6)` para gerar valores aleatórios, demonstrando o uso prático de simulações estatísticas. Ao final, o programa exibe uma análise estatística com a quantidade e o percentual de ocorrências da soma igual a 7, apresentando os resultados de forma organizada e profissional.

Atividade 19 — O programa foi desenvolvido em Python utilizando métodos de string da biblioteca padrão, conforme requisito obrigatório da questão. A resolução atende a todos os requisitos, realizando uma análise linguística completa da frase informada pelo usuário. O código utiliza o método `split()` para separar palavras, `len()` para contagem de caracteres e palavras, e `count()` com conversão para minúsculas (`lower()`) para contagem de ocorrências de letras. A implementação trata adequadamente espaços extras (múltiplos espaços, espaços no início e no final) utilizando `" ".join(frase.split())` para normalizar a frase. Os resultados exibidos incluem quantidade total de caracteres, quantidade de palavras, primeira e última palavra, ocorrências de uma letra escolhida, e a frase em maiúsculas e minúsculas, demonstrando o domínio das operações com strings.

Atividade 20 — O programa foi desenvolvido em Python como um projeto integrador completo, consolidando todos os conceitos estudados ao longo da disciplina. A resolução atende a todos os requisitos funcionais e técnicos, implementando um sistema acadêmico com menu interativo que oferece as operações: cadastrar estudante, listar estudantes, consultar estudante, alterar dados, remover estudante e gerar relatório da turma.

A arquitetura do código é modular, com funções específicas para cada funcionalidade, incluindo funções auxiliares para validações (notas, idade), cálculo de média e determinação de situação acadêmica. O sistema utiliza uma lista de dicionários como estrutura de dados principal, empregando todos os conceitos solicitados:

- **Variáveis e tipos primitivos**: strings, inteiros, floats e booleanos.
- **Operadores**: aritméticos (`+`, `/`), relacionais (`==`, `>=`, `<`) e lógicos (`and`, `or`, `not`).
- **Estruturas condicionais**: `if/elif/else` para controle de fluxo e tomada de decisões.
- **Estruturas de repetição**: `for` e `while` para iterações e validações.
- **Listas, dicionários**: para armazenamento e manipulação de dados dos estudantes.
- **Funções**: para modularização, reutilização e organização do código.
- **Bibliotecas padrão**: `os` para limpeza de tela e `time` para pequenas pausas, melhorando a experiência do usuário.

O sistema oferece uma experiência completa com validações robustas (notas entre 0 e 10, idade positiva, nome não vazio), mensagens informativas, confirmação para remoção de dados e relatórios estatísticos detalhados. Este projeto integrador demonstra plenamente a aplicação prática de todos os conceitos estudados, sendo adequado como atividade avaliativa de maior peso.
